import React from 'react';
import './Header.css';
import App from '../App';

function Header() {
    return(
        <header className="App-header">
            <h1>Book Recoomendation System</h1>
        </header>
    );
}

export default Header;